# Tự động thêm Prometheus làm datasource khi Grafana khởi động — khỏi phải click tay qua UI.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
